language: python
python:
  - "2.7"
virtualenv:
  system_site_packages: true
before_install:
  - sudo apt-get install -qq python-numpy python-scipy libhdf5-dev libgdal-dev libblas-dev liblapack-dev gfortran
install:
  - pip install -r requirements.txt
script: py.test

